import './App.css';
import WellWishes from './Components/WellWishes';

function App() {
  return (
    <div
      className="App"
      style={{ width: window.innerWidth, height: window.innerHeight }}
    >
      <WellWishes />
    </div>
  );
}

export default App;
